# 项目介绍
这是一个包含所有中国菜的食谱，八大菜系，对应菜系的所有菜谱。用于快速查询、收藏菜谱。

## 项目结构
- 项目根目录/
    - index.html
    - assets/
        - css/
        - js/
            - 页面主js
            - recipe/
                - 各个菜系对应英文的目录/（共八个）
                    - 一个js文件，存储各个菜谱的内容，充当数据库作用（共一个，存储当前菜系所有内容）
        - img/
            - 暂时留空

## 主要功能
- 主界面可下方选择各大菜系，进入后浏览所有菜谱
- 也可在主界面顶部搜索菜谱，结构需要按照菜系分类显示，触发搜索时指定了对应菜系
- 每个菜谱需要包含菜谱名称、菜谱图片、菜谱食材、菜谱调料、菜谱做法等信息
    - 对应细节补充
        - 食材：需要对应的名称，用量，单位等信息
        - 调料：需要对应的名称，用量，单位等信息
        - 做法：可通过提取对应菜谱js中存储的步骤，由前端界面逐步拆分，引导式展示
- 用户可手动点击收藏菜谱，收藏内容可在主页收藏部分展示
    - 由于项目为静态网页，所以收藏内容需要缓存至浏览器

## 项目技术栈
- 静态网页，后续由用户手动托管至github.io
- html+css+js
- 前端框架自行搭建，暂不适用任何现有前端框架

## 项目构建
- 构建分为前端界面、菜谱js数据库搭建两部分
- 实现初期可先行创建测试用js文件，包含2-3道菜品用于测试界面自动识别填充
- 随后开始搭建界面，包括主界面、菜谱详情页、收藏页等
- 界面交互、过渡、展示都需要过渡动画，界面不可出现卡片叠加卡片，蓝紫色配色的ai味设计